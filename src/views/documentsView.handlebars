<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>{{title}}</title>
</head>
<body>
    <header class="bg-light py-3">
        <div class="container">
            <div class="d-flex align-items-center">
                <img src="/img/main-logo.png" alt="Main Logo" class="me-3">
                <div class="flex-grow-1 text-center">
                    <h2>Upload Documents</h2>
                </div>
                <div class="d-flex align-items-center">
                    <div class="me-3">Welcome {{#if isLoggedIn}}{{user}}{{else}}user{{/if}}!</div>
                    <small class="me-3">{{#if isLoggedIn}}Role: {{role}}{{/if}}</small>
                    {{#if isLoggedIn}}
                        <a href="/logout" class="btn btn-outline-danger">Logout</a>
                    {{else}}
                        <a href="/login" class="btn btn-outline-primary">Login</a>
                    {{/if}}
                </div>
            </div>
        </div>
    </header>

    <main class="container my-4">
        <h2 class="text-center">Upload Your Documents</h2>
        <div id="userId" data-user-id="{{userId}}" class="mt-4">
            <form id="uploadForm" class="row g-3">
                <div class="col-md-6">
                    <label for="profileImage" class="form-label">Profile Image:</label>
                    <input type="file" id="profileImage" name="profileImage" class="form-control">
                </div>
                <div class="col-md-6">
                    <label for="productImage" class="form-label">Products Image:</label>
                    <input type="file" id="productImage" name="productImage" class="form-control">
                </div>
                <div class="col-md-6">
                    <label for="idDocument" class="form-label">ID Document:</label>
                    <input type="file" id="idDocument" class="form-control" name="idDocument">
                </div>
                <div class="col-md-6">
                    <label for="addressDocument" class="form-label">Address Document:</label>
                    <input type="file" id="addressDocument" class="form-control" name="addressDocument">
                </div>
                <div class="col-md-6">
                    <label for="statementDocument" class="form-label">Bank Statement:</label>
                    <input type="file" id="statementDocument" class="form-control" name="statementDocument">
                </div>
                <div class="col-12 text-center">
                    <button type="submit" class="btn btn-primary">Upload Files</button>
                </div>
            </form>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/js/uploader.js"></script>
</body>
</html>
