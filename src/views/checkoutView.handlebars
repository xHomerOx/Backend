<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Checkout Information</title>
</head>
<body>
    <header class="bg-light py-3">
        <div class="container">
            <div class="d-flex align-items-center">
                <img src="/img/main-logo.png" alt="Main Logo" class="me-3">
                <div class="flex-grow-1 text-center">
                    <h2>Checkout Information</h2>
                </div>

                <div class="d-flex align-items-center">
                    <a href="/logout" class="btn btn-outline-danger me-2">Logout</a>
                    <a href="/register" class="btn btn-outline-primary">Register</a>
                </div>
                
            </div>
        </div>
    </header>

    <section class="bg-light py-4">
        <div class="container">
            <div class="card shadow-sm rounded-3">
                <div class="card-body">
                    <h3 class="mb-4">Order Summary</h3>

                    <ul id="product-list" class="list-group mb-4" data-cart-id="{{cartId}}">
                        {{#each cart.products}}
                            <li class="list-group-item">
                                <strong>{{this.name}}</strong> - 
                                Quantity: {{this.quantity}}, 
                                Price: ${{this.price}} each
                            </li>
                        {{/each}}
                    </ul>

                    <p><strong>Total Amount:</strong> ${{amount}}</p>
    
                    <button id="confirm-purchase" type="button" class="btn btn-success" data-cart-id="{{cartId}}">Confirm Purchase</button>
                </div>
            </div>
        </div>
    </section>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/js/index.js"></script>
</body>
</html>
